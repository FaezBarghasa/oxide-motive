[package]
name = "oxide-protocol"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
postcard = { version = "1.0", features = ["use-std"] }
heapless = "0.8"
cobs = "0.2.3"

[dev-dependencies]
divan = "0.1.13"
