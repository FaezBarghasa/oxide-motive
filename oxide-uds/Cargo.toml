[package]
name = "oxide-uds"
version = "0.1.0"
edition = "2021"

[dependencies]
oxide-core = { path = "../oxide-core" }
embassy-executor = "0.5.0"
embassy-sync = "0.5.0"
embassy-time = "0.3.0"
mqtt-async-embedded = { path = "../mqtt-async-embedded" }

[features]
default = []